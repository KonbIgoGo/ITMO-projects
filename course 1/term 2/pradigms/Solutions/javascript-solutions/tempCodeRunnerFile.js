if (operations.get(ops[ops.length-1]).length == args.length && args.length != 0) {
                //     const res = operation.get(ops.pop())(...args.slice(args.length - operations.get(token).length));
                //     args = args.slice(0, args.length - operations.get(token).length)
                //     args.push(res);
                // } 